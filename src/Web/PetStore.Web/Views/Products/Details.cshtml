@model PetStore.Web.ViewModels.Products.ProductDetailsVieModels

@{
    ViewData["Title"] = "Details";
}

<h3 style="text-align:left">@Model.CategoryName</h3>
<h1 style="text-align:center">@Model.Name</h1>
<div class="d-flex justify-content-center container mt-3">
        <div class="card p-3 bg-white"><i class="fa fa-apple"></i>
            <div class="about-product text-center mt-2"><img src="@Model.ImageUrl" width="400">
                <div> <h6 class="mt-4 text-black-50">@Model.Description</h6></div>
            <div>Price: $@Math.Round(Model.Price, 2)</div>
            <div><a class="btn btn-success" asp-action="Products">Purchase</a> </div>
            <div style="text-align:right"> <a class="btn btn-dark" asp-action="Products">Back</a></div>
          </div>
       </div>
    </div>
<div/>

